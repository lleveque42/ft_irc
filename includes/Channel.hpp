#ifndef CHANNEL_HPP
# define CHANNEL_HPP

class Channel
{
	public:
		Channel(void);
		Channel();
		Channel(Channel const &instance);
		Channel &operator=(Channel const &rhs);
		~Channel(void);

	private:

};

#endif
